import { Navigate, NavLink } from 'react-router-dom'
import { loadBrandConfig } from '../lib/brand-config'

const navItems = [['Overview', '⌂'], ['Prompts', '⌁'], ['Sources', '◌'], ['Settings', '⚙']] as const

export default function DashboardPage() {
  const config = loadBrandConfig(); if (!config) return <Navigate to="/onboarding" replace />
  return <div className="dashboard-shell"><aside className="sidebar"><div className="auth-brand"><div className="brand-mark">G</div><span>GeoSuite</span></div><div className="workspace-switcher"><span className="avatar">{config.brandName.charAt(0).toUpperCase()}</span><span><strong>{config.brandName}</strong><small>Workspace</small></span><span>⌄</span></div><nav>{navItems.map(([label, icon], index) => <NavLink key={label} to={index === 0 ? '/dashboard' : `/${label.toLowerCase()}`} className={({ isActive }) => isActive ? 'active' : ''}><span>{icon}</span>{label}</NavLink>)}</nav><div className="sidebar-bottom"><div className="help-card"><span>✦</span><strong>Need a hand?</strong><small>Read the measurement guide</small></div><button className="profile-chip"><span className="avatar muted-avatar">JD</span><span><strong>Jordan Davis</strong><small>Free workspace</small></span><span>⋮</span></button></div></aside><main className="dashboard-main"><header className="dashboard-header"><div><div className="breadcrumb">Overview <span>/</span> AI visibility</div><h1>Good morning, Jordan <span>✦</span></h1><p className="muted">Here’s how {config.brandName} is showing up in AI search.</p></div><div className="header-actions"><button className="icon-button">?</button><button className="date-button">Last 30 days <span>⌄</span></button></div></header><section className="collecting-banner" role="status" aria-live="polite"><div className="pulse-icon"><span>◒</span></div><div><strong>Collecting your first AI visibility data</strong><p>We’re scanning AI answers for {config.brandName}. Your first results should be ready within 10 minutes.</p><div className="progress-track"><span /></div></div><span className="banner-time">Started just now</span></section><section className="section-heading"><div><h2>Your visibility at a glance</h2><p className="muted">Metrics will appear here once your first scan is complete.</p></div><button className="button secondary small">+ Add prompt</button></section><div className="kpi-grid">{['Visibility score', 'Mention rate', 'Share of voice', 'Sentiment'].map((item, i) => <article className="metric-card" key={item}><div className="metric-head"><span>{item}</span><span className="info">i</span></div><div className="skeleton metric-value" /><div className="metric-foot"><span className="skeleton short-line" /><span className="metric-period">vs. previous period</span></div></article>)}</div><div className="dashboard-grid"><article className="panel chart-panel"><div className="panel-header"><div><h3>Visibility trend</h3><p className="muted">Track how often your brand is mentioned over time.</p></div><span className="skeleton select-skeleton" /></div><div className="chart-empty"><div className="chart-lines"><span /><span /><span /><span /></div><div className="empty-center"><span className="empty-icon">⌁</span><strong>Your trend will appear here</strong><small>We need a little more data before we can show movement.</small></div></div></article><article className="panel sources-panel"><div className="panel-header"><div><h3>AI sources</h3><p className="muted">Where your brand is being surfaced.</p></div><button className="text-button">View all →</button></div><div className="source-empty"><span className="empty-icon">◌</span><strong>No sources yet</strong><small>Sources will populate after your first scan.</small></div></article></div></main></div>
}
